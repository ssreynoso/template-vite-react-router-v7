// Registro central de claves para queries de TanStack Query.
// Definí acá las keys de cada módulo para reusarlas en hooks e invalidaciones.
//
// Ejemplo:
//   export const queryKeys = {
//       users: ['users'] as const,
//       user: (id: string) => ['users', id] as const
//   }
export const queryKeys = {} as const
